// Copy this file to env.js and update values before deploying the frontend.
// Place <script src="/env.js"></script> in your HTML BEFORE other scripts to set runtime values.

// Socket endpoint used by client when frontend is hosted separately (e.g., Vercel).
// Set to your Render web service domain (example: https://annasetu-backend.onrender.com)
window.__SOCKET_ENDPOINT__ = 'https://RENDER_SERVICE.onrender.com';

// If you prefer to hardcode an API URL instead of using Vercel rewrites, set API_PREFIX.
// Leave as empty string to use relative "/api" paths (recommended with Vercel rewrites).
window.__API_PREFIX__ = '';
